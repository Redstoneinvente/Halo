name: One-time Drop CI polish

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/one-time-drop-ci-polish.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Polish Drop CI and remove Retro mascot
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path
          import re

          def replace_once(path, old, new, label):
              p = Path(path)
              text = p.read_text()
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{label}: expected 1 match in {path}, found {count}")
              p.write_text(text.replace(old, new, 1))

          def replace_between(path, start, end, replacement, label):
              p = Path(path)
              text = p.read_text()
              a = text.find(start)
              if a < 0:
                  raise SystemExit(f"{label}: start marker missing in {path}")
              b = text.find(end, a)
              if b < 0:
                  raise SystemExit(f"{label}: end marker missing in {path}")
              p.write_text(text[:a] + replacement + text[b:])

          display = Path("Halo/NotchEngine/DisplayClock.swift")
          text = display.read_text()
          old = '''    mutating func normalize() {
              if zones.count > 8 { zones = Array(zones.prefix(8)) }
'''
          new = '''    mutating func normalize() {
              if zones.isEmpty { zones = [.preset(.shelf, index: 0)] }
              if zones.count > 8 { zones = Array(zones.prefix(8)) }
'''
          if old not in text:
              raise SystemExit("Drop config normalize marker missing")
          text = text.replace(old, new, 1)
          old = '''    func removeZone(at index: Int) {
              guard configuration.zones.indices.contains(index) else { return }
'''
          new = '''    func removeZone(at index: Int) {
              guard configuration.zones.count > 1, configuration.zones.indices.contains(index) else { return }
'''
          if old not in text:
              raise SystemExit("removeZone marker missing")
          text = text.replace(old, new, 1)
          display.write_text(text)

          board = r'''@MainActor
          private struct HaloDropZoneBoardView: View {
              @ObservedObject var settings: HaloDropZoneSettingsStore
              @ObservedObject var model: HaloDropZoneRuntimeModel

              var body: some View {
                  GeometryReader { proxy in
                      let configuration = settings.configuration
                      let frames = HaloDropZoneLayoutResolver.frames(size: proxy.size, configuration: configuration)
                      let compact = configuration.zones.count > 4 || proxy.size.height < 275

                      ZStack(alignment: .topLeading) {
                          Color.black.opacity(configuration.backgroundOpacity)
                          LinearGradient(
                              colors: [Color.white.opacity(0.045), Color.accentColor.opacity(0.055), Color.clear],
                              startPoint: .topLeading,
                              endPoint: .bottomTrailing
                          )
                          .allowsHitTesting(false)

                          HStack(spacing: 10) {
                              ZStack {
                                  RoundedRectangle(cornerRadius: 9, style: .continuous)
                                      .fill(Color.white.opacity(0.075))
                                  Image(systemName: "arrow.down.doc.fill")
                                      .font(.system(size: 14, weight: .semibold))
                                      .foregroundStyle(Color.accentColor)
                              }
                              .frame(width: 32, height: 32)

                              VStack(alignment: .leading, spacing: 1) {
                                  Text(configuration.headerTitle.isEmpty ? "Drop" : configuration.headerTitle)
                                      .font(.system(size: 15, weight: .semibold, design: .rounded))
                                  Text(model.result ?? (configuration.headerSubtitle.isEmpty ? "Choose an action" : configuration.headerSubtitle))
                                      .font(.system(size: 9.5, weight: .medium))
                                      .foregroundStyle(model.result == nil ? Color.white.opacity(0.48) : Color.green)
                                      .lineLimit(1)
                              }
                              Spacer(minLength: 8)
                              HStack(spacing: 5) {
                                  Image(systemName: model.itemCount == 1 ? "doc.fill" : "doc.on.doc.fill")
                                  Text("\(model.itemCount)")
                              }
                              .font(.system(size: 9, weight: .semibold, design: .rounded))
                              .foregroundStyle(.white.opacity(0.68))
                              .padding(.horizontal, 8)
                              .padding(.vertical, 5)
                              .background(Color.white.opacity(0.065), in: Capsule())
                          }
                          .padding(.horizontal, max(13, configuration.boardPadding + 3))
                          .padding(.top, 11)

                          ForEach(Array(configuration.zones.enumerated()), id: \.element.id) { index, zone in
                              if frames.indices.contains(index) {
                                  let frame = frames[index]
                                  zoneCard(zone, active: model.hoveredZone == index, configuration: configuration, compact: compact)
                                      .frame(width: frame.width, height: frame.height)
                                      .position(x: frame.midX, y: frame.midY)
                              }
                          }

                          HStack(spacing: 6) {
                              Circle()
                                  .fill(model.hoveredZone == nil ? Color.white.opacity(0.24) : Color.green)
                                  .frame(width: 5, height: 5)
                              Text(model.hoveredZone == nil ? "Choose a zone" : "Release to run action")
                                  .font(.system(size: 8, weight: .medium, design: .rounded))
                                  .foregroundStyle(.white.opacity(0.42))
                          }
                          .position(x: proxy.size.width / 2, y: max(12, proxy.size.height - 10))
                      }
                      .foregroundStyle(.white)
                  }
              }

              private func zoneCard(
                  _ zone: HaloDropZone,
                  active: Bool,
                  configuration: HaloDropZoneConfiguration,
                  compact: Bool
              ) -> some View {
                  GeometryReader { proxy in
                      let veryCompact = compact || proxy.size.height < 78 || proxy.size.width < 105
                      VStack(alignment: .leading, spacing: veryCompact ? 4 : 7) {
                          HStack(alignment: .center, spacing: 8) {
                              ZStack {
                                  RoundedRectangle(cornerRadius: veryCompact ? 8 : 10, style: .continuous)
                                      .fill(zone.color.color.opacity(active ? 0.22 : 0.11))
                                  Image(systemName: zone.symbol.isEmpty ? zone.action.symbol : zone.symbol)
                                      .font(.system(size: veryCompact ? 13 : 16, weight: .semibold))
                                      .foregroundStyle(active ? Color.white : zone.color.color)
                              }
                              .frame(width: veryCompact ? 28 : 34, height: veryCompact ? 28 : 34)

                              VStack(alignment: .leading, spacing: 1) {
                                  Text(zone.title.isEmpty ? zone.action.rawValue : zone.title)
                                      .font(.system(size: veryCompact ? 9.5 : 11.5, weight: .semibold, design: .rounded))
                                      .lineLimit(1)
                                  if configuration.showSubtitles && !veryCompact && !zone.subtitle.isEmpty {
                                      Text(zone.subtitle)
                                          .font(.system(size: 8.3, weight: .medium))
                                          .foregroundStyle(.white.opacity(0.43))
                                          .lineLimit(1)
                                  }
                              }
                              Spacer(minLength: 0)
                              if active {
                                  Image(systemName: "arrow.down.circle.fill")
                                      .font(.system(size: veryCompact ? 12 : 15, weight: .semibold))
                                      .foregroundStyle(zone.color.color)
                                      .transition(.scale.combined(with: .opacity))
                              } else if zone.action.isDestructive {
                                  Image(systemName: "exclamationmark.triangle.fill")
                                      .font(.system(size: 9, weight: .semibold))
                                      .foregroundStyle(.orange.opacity(0.82))
                              }
                          }

                          if !veryCompact {
                              Spacer(minLength: 0)
                              HStack(spacing: 6) {
                                  if configuration.showActionBadges {
                                      Label(zone.accepts.rawValue, systemImage: zone.accepts.symbol)
                                          .font(.system(size: 7.5, weight: .semibold))
                                          .foregroundStyle(.white.opacity(0.45))
                                  }
                                  Spacer(minLength: 0)
                                  Text(active ? "RELEASE" : zone.action.rawValue.uppercased())
                                      .font(.system(size: 7.2, weight: .bold, design: .rounded))
                                      .foregroundStyle(active ? zone.color.color : Color.white.opacity(0.28))
                                      .lineLimit(1)
                              }
                          }
                      }
                      .padding(veryCompact ? 7 : 10)
                      .frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .topLeading)
                      .background(
                          ZStack {
                              RoundedRectangle(cornerRadius: configuration.cornerRadius, style: .continuous)
                                  .fill(Color.white.opacity(active ? 0.085 : 0.045))
                              LinearGradient(
                                  colors: [zone.color.color.opacity(active ? 0.17 : 0.045), Color.clear],
                                  startPoint: .topLeading,
                                  endPoint: .bottomTrailing
                              )
                              .clipShape(RoundedRectangle(cornerRadius: configuration.cornerRadius, style: .continuous))
                          }
                      )
                      .overlay(
                          RoundedRectangle(cornerRadius: configuration.cornerRadius, style: .continuous)
                              .stroke(active ? zone.color.color.opacity(configuration.highlightStrength) : Color.white.opacity(0.075), lineWidth: active ? 1.6 : 1)
                      )
                      .overlay(alignment: .top) {
                          Capsule()
                              .fill(zone.color.color.opacity(active ? 0.95 : 0.42))
                              .frame(height: active ? 2.5 : 1.5)
                              .padding(.horizontal, max(10, configuration.cornerRadius * 0.6))
                      }
                      .shadow(color: zone.color.color.opacity(active ? 0.18 : 0), radius: active ? 14 : 0, y: 4)
                      .scaleEffect(active ? 1.012 : 1)
                      .animation(.easeOut(duration: 0.12), value: active)
                  }
              }
          }

          '''
          replace_between(
              "Halo/NotchEngine/DisplayClock.swift",
              "@MainActor\nprivate struct HaloDropZoneBoardView: View {",
              "@MainActor\nprivate final class HaloDropZoneHostView: NSView {",
              board,
              "Drop board"
          )

          editor = r'''@MainActor
          private struct HaloDropZoneStudioView: View {
              var body: some View {
                  ScrollView {
                      HaloDropZoneSettingsEditor()
                          .padding(18)
                  }
                  .frame(minWidth: 700, minHeight: 600)
              }
          }

          @MainActor
          struct HaloDropZoneSettingsEditor: View {
              @ObservedObject private var store = HaloDropZoneSettingsStore.shared

              var body: some View {
                  VStack(alignment: .leading, spacing: 16) {
                      HStack(spacing: 10) {
                          VStack(alignment: .leading, spacing: 2) {
                              Text("Drop zones").font(.headline)
                              Text("Build the action board that appears inside Drop CI.")
                                  .font(.caption).foregroundStyle(.secondary)
                          }
                          Spacer()
                          Button("Reset") { store.reset() }
                          Button { store.addZone() } label: { Label("Add Zone", systemImage: "plus") }
                              .disabled(store.configuration.zones.count >= 8)
                      }

                      HaloDropZoneStaticPreview(configuration: store.configuration)
                          .frame(height: previewHeight)

                      GroupBox("Layout") {
                          VStack(alignment: .leading, spacing: 10) {
                              HStack(spacing: 14) {
                                  Picker("Zones", selection: zoneCountBinding) {
                                      ForEach(1...8, id: \.self) { Text("\($0)").tag($0) }
                                  }
                                  Picker("Arrangement", selection: configurationBinding(\.layout)) {
                                      ForEach(HaloDropZoneLayout.allCases) {
                                          Label($0.rawValue, systemImage: $0.symbol).tag($0)
                                      }
                                  }
                              }
                              HStack {
                                  valueSlider("Padding", \.boardPadding, 0...28)
                                  valueSlider("Spacing", \.zoneSpacing, 2...24)
                              }
                              HStack {
                                  valueSlider("Corner radius", \.cornerRadius, 6...36)
                                  valueSlider("Background", \.backgroundOpacity, 0.45...1)
                              }
                              valueSlider("Hover emphasis", \.highlightStrength, 0.15...1)
                              HStack(spacing: 18) {
                                  Toggle("Icons", isOn: configurationBinding(\.showIcons))
                                  Toggle("Subtitles", isOn: configurationBinding(\.showSubtitles))
                                  Toggle("Type badges", isOn: configurationBinding(\.showActionBadges))
                              }
                              TextField("Header title", text: configurationBinding(\.headerTitle))
                              TextField("Header instruction", text: configurationBinding(\.headerSubtitle))
                          }
                          .padding(.vertical, 4)
                      }

                      GroupBox("Zone actions") {
                          VStack(spacing: 10) {
                              ForEach(Array(store.configuration.zones.enumerated()), id: \.element.id) { index, _ in
                                  zoneEditor(index)
                              }
                          }
                          .padding(.vertical, 4)
                      }

                      Text("Drop CI supports up to 8 zones. Rename and Move to Trash modify originals; Halo never assigns destructive actions automatically.")
                          .font(.caption).foregroundStyle(.secondary)
                  }
              }

              private var previewHeight: CGFloat {
                  let count = store.configuration.zones.count
                  let rows: Int
                  switch store.configuration.layout {
                  case .vertical: rows = count
                  case .horizontal: rows = 1
                  case .twoColumns: rows = Int(ceil(Double(count) / 2.0))
                  case .threeColumns: rows = Int(ceil(Double(count) / 3.0))
                  case .fourColumns: rows = Int(ceil(Double(count) / 4.0))
                  case .spotlight: rows = count > 4 ? 2 : 1
                  case .adaptive:
                      let columns = count <= 2 ? count : count <= 4 ? 2 : count <= 6 ? 3 : 4
                      rows = Int(ceil(Double(count) / Double(max(1, columns))))
                  }
                  return min(420, max(230, 132 + CGFloat(rows) * 68))
              }

              private func zoneEditor(_ index: Int) -> some View {
                  let zone = zoneBinding(index)
                  return DisclosureGroup {
                      VStack(alignment: .leading, spacing: 10) {
                          HStack {
                              TextField("Zone title", text: zone.title)
                              TextField("SF Symbol", text: zone.symbol).frame(width: 180)
                          }
                          TextField("Subtitle", text: zone.subtitle)
                          HStack {
                              Picker("Action", selection: zone.action) {
                                  ForEach(HaloDropZoneAction.allCases) {
                                      Label($0.rawValue, systemImage: $0.symbol).tag($0)
                                  }
                              }
                              Picker("Accept", selection: zone.accepts) {
                                  ForEach(HaloDropZoneAcceptance.allCases) {
                                      Label($0.rawValue, systemImage: $0.symbol).tag($0)
                                  }
                              }
                          }
                          ColorPicker(
                              "Accent color",
                              selection: Binding(
                                  get: { zone.wrappedValue.color.color },
                                  set: { color in
                                      var next = zone.wrappedValue
                                      next.color = WidgetColor(color)
                                      zone.wrappedValue = next
                                  }
                              ),
                              supportsOpacity: false
                          )

                          if zone.wrappedValue.action == .rename {
                              TextField("Rename template", text: zone.parameter)
                              Text("Use {name}, {ext}, and {date}.").font(.caption).foregroundStyle(.secondary)
                          } else if zone.wrappedValue.action == .copyFolder {
                              HStack {
                                  TextField("Destination folder", text: zone.parameter)
                                  Button("Choose…") { chooseFolder(for: index) }
                              }
                          }

                          if zone.wrappedValue.action != .shelf {
                              Toggle("Also add accepted items to File Shelf", isOn: zone.alsoAddToShelf)
                          }
                          if zone.wrappedValue.action.isDestructive {
                              Label("This action changes the original item.", systemImage: "exclamationmark.triangle.fill")
                                  .font(.caption).foregroundStyle(.orange)
                          }
                          HStack {
                              Button { store.moveZone(from: index, by: -1) } label: { Label("Earlier", systemImage: "arrow.up") }
                                  .disabled(index == 0)
                              Button { store.moveZone(from: index, by: 1) } label: { Label("Later", systemImage: "arrow.down") }
                                  .disabled(index == store.configuration.zones.count - 1)
                              Button { duplicateZone(index) } label: { Label("Duplicate", systemImage: "plus.square.on.square") }
                                  .disabled(store.configuration.zones.count >= 8)
                              Spacer()
                              Button(role: .destructive) { store.removeZone(at: index) } label: {
                                  Label("Remove", systemImage: "trash")
                              }
                              .disabled(store.configuration.zones.count <= 1)
                          }
                      }
                      .padding(.top, 8)
                  } label: {
                      HStack {
                          ZStack {
                              RoundedRectangle(cornerRadius: 7, style: .continuous)
                                  .fill(zone.wrappedValue.color.color.opacity(0.14))
                              Image(systemName: zone.wrappedValue.symbol.isEmpty ? zone.wrappedValue.action.symbol : zone.wrappedValue.symbol)
                                  .foregroundStyle(zone.wrappedValue.color.color)
                          }
                          .frame(width: 28, height: 28)
                          VStack(alignment: .leading, spacing: 1) {
                              Text(zone.wrappedValue.title.isEmpty ? zone.wrappedValue.action.rawValue : zone.wrappedValue.title)
                                  .font(.headline)
                              Text(zone.wrappedValue.action.rawValue)
                                  .font(.caption2).foregroundStyle(.secondary)
                          }
                          Spacer()
                          Text(zone.wrappedValue.accepts.rawValue).font(.caption2).foregroundStyle(.secondary)
                      }
                  }
              }

              private var zoneCountBinding: Binding<Int> {
                  Binding(
                      get: { store.configuration.zones.count },
                      set: { requested in
                          let count = min(8, max(1, requested))
                          while store.configuration.zones.count < count { store.addZone() }
                          while store.configuration.zones.count > count { store.removeZone(at: store.configuration.zones.count - 1) }
                      }
                  )
              }

              private func configurationBinding<T>(_ keyPath: WritableKeyPath<HaloDropZoneConfiguration, T>) -> Binding<T> {
                  Binding(
                      get: { store.configuration[keyPath: keyPath] },
                      set: { value in
                          var next = store.configuration
                          next[keyPath: keyPath] = value
                          next.normalize()
                          store.configuration = next
                      }
                  )
              }

              private func zoneBinding(_ index: Int) -> Binding<HaloDropZone> {
                  Binding(
                      get: { store.configuration.zones[index] },
                      set: { value in
                          guard store.configuration.zones.indices.contains(index) else { return }
                          var next = store.configuration
                          next.zones[index] = value
                          next.normalize()
                          store.configuration = next
                      }
                  )
              }

              private func duplicateZone(_ index: Int) {
                  guard store.configuration.zones.indices.contains(index), store.configuration.zones.count < 8 else { return }
                  var next = store.configuration
                  var copy = next.zones[index]
                  copy.id = UUID()
                  copy.title = copy.title.isEmpty ? copy.action.rawValue : copy.title + " Copy"
                  next.zones.insert(copy, at: min(index + 1, next.zones.count))
                  next.normalize()
                  store.configuration = next
              }

              private func chooseFolder(for index: Int) {
                  let panel = NSOpenPanel()
                  panel.canChooseFiles = false
                  panel.canChooseDirectories = true
                  panel.allowsMultipleSelection = false
                  if panel.runModal() == .OK, let url = panel.url {
                      var next = store.configuration
                      next.zones[index].parameter = url.path
                      store.configuration = next
                  }
              }

              private func valueSlider(
                  _ title: String,
                  _ keyPath: WritableKeyPath<HaloDropZoneConfiguration, Double>,
                  _ range: ClosedRange<Double>
              ) -> some View {
                  VStack(alignment: .leading, spacing: 3) {
                      HStack {
                          Text(title)
                          Spacer()
                          Text(String(format: "%.1f", store.configuration[keyPath: keyPath]))
                              .font(.caption.monospacedDigit()).foregroundStyle(.secondary)
                      }
                      SwiftUI.Slider(value: configurationBinding(keyPath), in: range)
                  }
              }
          }

          '''
          replace_between(
              "Halo/NotchEngine/DisplayClock.swift",
              "@MainActor\nprivate struct HaloDropZoneStudioView: View {",
              "@MainActor\nprivate struct HaloDropZoneStaticPreview: View {",
              editor,
              "Drop settings editor"
          )

          preview = r'''@MainActor
          private struct HaloDropZoneStaticPreview: View {
              let configuration: HaloDropZoneConfiguration

              var body: some View {
                  GeometryReader { proxy in
                      let frames = HaloDropZoneLayoutResolver.frames(size: proxy.size, configuration: configuration)
                      let compact = configuration.zones.count > 4 || proxy.size.height < 275
                      ZStack(alignment: .topLeading) {
                          Color.black.opacity(configuration.backgroundOpacity)
                          LinearGradient(colors: [Color.white.opacity(0.04), Color.accentColor.opacity(0.05), Color.clear], startPoint: .topLeading, endPoint: .bottomTrailing)

                          HStack(spacing: 8) {
                              RoundedRectangle(cornerRadius: 8, style: .continuous)
                                  .fill(Color.white.opacity(0.07))
                                  .frame(width: 29, height: 29)
                                  .overlay(Image(systemName: "arrow.down.doc.fill").font(.system(size: 13, weight: .semibold)).foregroundStyle(Color.accentColor))
                              VStack(alignment: .leading, spacing: 1) {
                                  Text(configuration.headerTitle.isEmpty ? "Drop" : configuration.headerTitle).font(.system(size: 14, weight: .semibold, design: .rounded))
                                  Text(configuration.headerSubtitle.isEmpty ? "Choose an action" : configuration.headerSubtitle).font(.system(size: 8.5)).foregroundStyle(.secondary).lineLimit(1)
                              }
                              Spacer()
                              Text("PREVIEW").font(.system(size: 7, weight: .bold, design: .rounded)).foregroundStyle(.secondary)
                          }
                          .padding(.horizontal, max(12, configuration.boardPadding + 2))
                          .padding(.top, 11)

                          ForEach(Array(configuration.zones.enumerated()), id: \.element.id) { index, zone in
                              if frames.indices.contains(index) {
                                  let frame = frames[index]
                                  VStack(alignment: .leading, spacing: compact ? 3 : 6) {
                                      HStack(spacing: 7) {
                                          RoundedRectangle(cornerRadius: 8, style: .continuous)
                                              .fill(zone.color.color.opacity(index == 0 ? 0.22 : 0.10))
                                              .frame(width: compact ? 25 : 30, height: compact ? 25 : 30)
                                              .overlay(Image(systemName: zone.symbol.isEmpty ? zone.action.symbol : zone.symbol).font(.system(size: compact ? 11 : 14, weight: .semibold)).foregroundStyle(zone.color.color))
                                          Text(zone.title.isEmpty ? zone.action.rawValue : zone.title)
                                              .font(.system(size: compact ? 8.5 : 10.5, weight: .semibold, design: .rounded)).lineLimit(1)
                                          Spacer(minLength: 0)
                                      }
                                      if !compact {
                                          Spacer(minLength: 0)
                                          Text(zone.action.rawValue.uppercased()).font(.system(size: 6.5, weight: .bold, design: .rounded)).foregroundStyle(.secondary).lineLimit(1)
                                      }
                                  }
                                  .padding(compact ? 6 : 9)
                                  .frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .topLeading)
                                  .background(Color.white.opacity(index == 0 ? 0.08 : 0.04), in: RoundedRectangle(cornerRadius: configuration.cornerRadius, style: .continuous))
                                  .overlay(RoundedRectangle(cornerRadius: configuration.cornerRadius, style: .continuous).stroke(index == 0 ? zone.color.color.opacity(0.72) : Color.white.opacity(0.07), lineWidth: index == 0 ? 1.5 : 1))
                                  .overlay(alignment: .top) { Capsule().fill(zone.color.color.opacity(index == 0 ? 0.9 : 0.4)).frame(height: index == 0 ? 2.5 : 1.5).padding(.horizontal, 10) }
                                  .frame(width: frame.width, height: frame.height)
                                  .position(x: frame.midX, y: frame.midY)
                              }
                          }
                      }
                      .clipShape(RoundedRectangle(cornerRadius: 18, style: .continuous))
                      .overlay(RoundedRectangle(cornerRadius: 18, style: .continuous).stroke(Color.white.opacity(0.08)))
                      .foregroundStyle(.white)
                  }
              }
          }

          '''
          replace_between(
              "Halo/NotchEngine/DisplayClock.swift",
              "@MainActor\nprivate struct HaloDropZoneStaticPreview: View {",
              "// MARK: - Global file-drag monitor",
              preview,
              "Drop preview"
          )

          monitor = r'''@MainActor
          private final class HaloGlobalFileDragMonitor {
              static let shared = HaloGlobalFileDragMonitor()

              private var globalMonitor: Any?
              private var localMonitor: Any?
              private var pollTimer: Timer?
              private weak var activeTarget: (any HaloGlobalDropTarget)?
              private var activeItemCount = 0
              private var dragSessionBaselineChangeCount: Int?
              private var lastCompletedPasteboardChangeCount: Int?
              private var sawMouseDrag = false
              private var deferredFinishPending = false

              private init() {}

              func start() {
                  guard globalMonitor == nil, localMonitor == nil else { return }
                  HaloDropZoneStudioWindowController.shared.installMenuItem()

                  let mask: NSEvent.EventTypeMask = [.leftMouseDown, .leftMouseDragged, .leftMouseUp]
                  globalMonitor = NSEvent.addGlobalMonitorForEvents(matching: mask) { [weak self] event in
                      let type = event.type
                      DispatchQueue.main.async { [weak self] in self?.handleMouseEvent(type) }
                  }
                  localMonitor = NSEvent.addLocalMonitorForEvents(matching: mask) { [weak self] event in
                      let type = event.type
                      DispatchQueue.main.async { [weak self] in self?.handleMouseEvent(type) }
                      return event
                  }

                  let timer = Timer(timeInterval: 0.05, repeats: true) { [weak self] _ in
                      Task { @MainActor [weak self] in self?.pollDragSession() }
                  }
                  timer.tolerance = 0.01
                  pollTimer = timer
                  RunLoop.main.add(timer, forMode: .common)
              }

              private func handleMouseEvent(_ type: NSEvent.EventType) {
                  switch type {
                  case .leftMouseDown:
                      beginPointerSession()
                  case .leftMouseDragged:
                      sawMouseDrag = true
                      inspectDragPasteboard()
                  case .leftMouseUp:
                      finishAfterDropOpportunity()
                  default:
                      break
                  }
              }

              private func beginPointerSession() {
                  let pasteboard = NSPasteboard(name: .drag)
                  dragSessionBaselineChangeCount = pasteboard.changeCount
                  sawMouseDrag = false
                  deferredFinishPending = false
              }

              private func pollDragSession() {
                  let leftButtonDown = (NSEvent.pressedMouseButtons & 1) != 0
                  guard leftButtonDown else {
                      if activeTarget != nil || sawMouseDrag { finishAfterDropOpportunity() }
                      return
                  }

                  deferredFinishPending = false
                  if dragSessionBaselineChangeCount == nil {
                      dragSessionBaselineChangeCount = NSPasteboard(name: .drag).changeCount
                  }
                  inspectDragPasteboard()

                  if activeTarget != nil {
                      activateTarget(at: NSEvent.mouseLocation, count: max(1, activeItemCount))
                  }
              }

              private func inspectDragPasteboard() {
                  let pasteboard = NSPasteboard(name: .drag)
                  let count = dragPasteboardFileCount(pasteboard)
                  guard count > 0 else { return }

                  // A real drag session writes a fresh payload to NSDragPboard. Requiring a
                  // change from the mouse-down baseline is what distinguishes dragging a file
                  // from moving/resizing a Finder window or any other ordinary mouse drag.
                  if activeTarget == nil {
                      if let baseline = dragSessionBaselineChangeCount,
                         pasteboard.changeCount == baseline { return }
                      if dragSessionBaselineChangeCount == nil,
                         let lastCompletedPasteboardChangeCount,
                         pasteboard.changeCount == lastCompletedPasteboardChangeCount { return }
                  }

                  activeItemCount = count
                  activateTarget(at: NSEvent.mouseLocation, count: count)
              }

              private func dragPasteboardFileCount(_ pasteboard: NSPasteboard = NSPasteboard(name: .drag)) -> Int {
                  pasteboard.pasteboardItems?.reduce(into: 0) { result, item in
                      if item.availableType(from: [.fileURL]) != nil { result += 1 }
                  } ?? 0
              }

              private func activateTarget(at point: NSPoint, count: Int) {
                  guard let target = targetForDrag(at: point) else { return }
                  if let activeTarget, activeTarget !== target {
                      activeTarget.dragStateHandler?(false, 0)
                      HaloEmbeddedDropZoneController.shared.dismiss()
                  }
                  activeTarget = target
                  target.dragStateHandler?(true, max(1, count))
                  HaloEmbeddedDropZoneController.shared.begin(target: target, itemCount: max(1, count))
              }

              private func targetForDrag(at point: NSPoint) -> (any HaloGlobalDropTarget)? {
                  let panels = NSApp.windows.compactMap { $0 as? HaloPanel }
                  if let panel = panels.first(where: { panel in
                      guard let screen = panel.screen else { return false }
                      return screen.frame.contains(point)
                  }), let target = panel.contentView as? any HaloGlobalDropTarget {
                      return target
                  }
                  return panels.compactMap { $0.contentView as? any HaloGlobalDropTarget }.first
              }

              private func finishAfterDropOpportunity() {
                  guard !deferredFinishPending else { return }
                  if HaloEmbeddedDropZoneController.shared.containsScreenPoint(NSEvent.mouseLocation) {
                      deferredFinishPending = true
                      DispatchQueue.main.asyncAfter(deadline: .now() + 0.30) { [weak self] in
                          guard let self else { return }
                          self.deferredFinishPending = false
                          if HaloEmbeddedDropZoneController.shared.dropHandled {
                              self.resetSessionState()
                          } else {
                              self.finishDrag()
                          }
                      }
                  } else {
                      finishDrag()
                  }
              }

              private func finishDrag() {
                  guard activeTarget != nil || sawMouseDrag else {
                      resetSessionState()
                      return
                  }
                  activeTarget?.dragStateHandler?(false, 0)
                  HaloEmbeddedDropZoneController.shared.cancelIfNeeded()
                  resetSessionState()
              }

              private func resetSessionState() {
                  activeTarget = nil
                  activeItemCount = 0
                  sawMouseDrag = false
                  dragSessionBaselineChangeCount = nil
                  lastCompletedPasteboardChangeCount = NSPasteboard(name: .drag).changeCount
              }
          }

          '''
          replace_between(
              "Halo/NotchEngine/DisplayClock.swift",
              "@MainActor\nprivate final class HaloGlobalFileDragMonitor {",
              "// MARK: - Display clock",
              monitor,
              "Global drag monitor"
          )

          # Surface size should respond to the selected Drop CI zone layout/count.
          replace_once(
              "Halo/Views/SurfaceView.swift",
              '''private struct DropContextView: View {
              let itemCount: Int
              @ObservedObject var surfaceState: SurfaceState
''',
              '''private struct DropContextView: View {
              let itemCount: Int
              @ObservedObject var surfaceState: SurfaceState
              @ObservedObject private var dropZones = HaloDropZoneSettingsStore.shared
''',
              "DropContextView store"
          )
          replace_once(
              "Halo/Views/SurfaceView.swift",
              '''    private var preferredSize: CGSize { CGSize(width: 520, height: 270 + max(0, topInset - 22)) }
''',
              '''    private var preferredSize: CGSize {
              let configuration = dropZones.configuration
              let count = max(1, configuration.zones.count)
              let columns: Int
              switch configuration.layout {
              case .vertical: columns = 1
              case .horizontal: columns = count
              case .twoColumns: columns = 2
              case .threeColumns: columns = 3
              case .fourColumns: columns = 4
              case .spotlight: columns = count > 4 ? 2 : 2
              case .adaptive:
                  columns = count <= 2 ? count : count <= 4 ? 2 : count <= 6 ? 3 : 4
              }
              let rows = max(1, Int(ceil(Double(count) / Double(max(1, columns)))))
              let width: Double
              switch configuration.layout {
              case .vertical: width = 520
              case .horizontal: width = min(760, max(560, 118 * Double(count)))
              case .spotlight: width = 680
              default: width = count >= 5 ? 680 : 600
              }
              let rowHeight = count > 4 ? 76.0 : 92.0
              let height = 112 + Double(rows) * rowHeight + Double(max(0, rows - 1)) * configuration.zoneSpacing + max(0, topInset - 22)
              return CGSize(width: width, height: min(700, max(270, height)))
          }
''',
              "DropContext preferred size"
          )
          replace_once(
              "Halo/Views/SurfaceView.swift",
              '''        .onChange(of: itemCount) { _ in publishPreferredSize() }
''',
              '''        .onChange(of: itemCount) { _ in publishPreferredSize() }
              .onChange(of: dropZones.configuration) { _ in publishPreferredSize() }
''',
              "DropContext configuration sizing"
          )

          # Put the full zone editor directly in Drop CI's normal settings page.
          replace_between(
              "Halo/Views/WorkspaceSettingsView.swift",
              "private struct ContextDropSettings: View {",
              "private struct ContextInterfaceCard: View {",
              r'''private struct ContextDropSettings: View {
              @AppStorage("HaloContextDropEnabled") private var enabled = true
              @AppStorage("HaloContextDropUseFullNotchArea") private var usesFullNotchArea = true
              @AppStorage("HaloContextDropKeepClosedNotchContents") private var keepsClosedNotchContents = false
              @AppStorage("HaloContextDropPriority") private var priority = 100.0

              var body: some View {
                  Section("Drag & Drop Context Interface") {
                      Toggle("Enable Drop CI", isOn: $enabled)
                      Text("Start dragging a real file or folder anywhere on your Mac and Halo can summon Drop CI immediately. The cursor does not need to reach the notch first.")
                          .font(.caption).foregroundStyle(.secondary)
                  }
                  Section("Drop zones") {
                      HaloDropZoneSettingsEditor()
                  }
                  Section("CI priority") {
                      Slider(value: $priority, in: 0...100, step: 1) { Text("Drop CI priority") }
                      Text("Drop CI defaults to the highest priority because a drag is an immediate user action. Lower it if another Context Interface should keep ownership during file drags.")
                          .font(.caption).foregroundStyle(.secondary)
                  }
                  Section("CI surface") {
                      Toggle("Use full notch area", isOn: $usesFullNotchArea)
                      Toggle("Keep closed-notch contents visible", isOn: $keepsClosedNotchContents)
                      Text("The Drop board lives inside the actual Drop CI surface. Halo automatically requests more vertical space when your chosen zone layout needs it.")
                          .font(.caption).foregroundStyle(.secondary)
                  }
                  Section("Drop behaviour") {
                      Label("Only genuine file/folder drag payloads can summon Drop CI.", systemImage: "checkmark.shield")
                      Label("Each zone decides what happens when you release over it.", systemImage: "square.grid.2x2")
                      Label("Destructive actions are never assigned automatically.", systemImage: "lock.shield")
                  }
              }
          }

          ''',
              "Drop settings page"
          )

          # Make the library card accurately describe the configurable action board.
          ws = Path("Halo/Views/WorkspaceSettingsView.swift")
          w = ws.read_text()
          old = '''    @AppStorage("HaloContextDropPriority") private var priority = 100.0
              @State private var hovered = false
'''
          new = '''    @AppStorage("HaloContextDropPriority") private var priority = 100.0
              @ObservedObject private var zones = HaloDropZoneSettingsStore.shared
              @State private var hovered = false
'''
          if old not in w:
              raise SystemExit("Drop card property marker missing")
          w = w.replace(old, new, 1)
          old = '''                Text("Turns Halo into a focused drop target while a Finder item is hovering over the notch.")
'''
          new = '''                Text("A customizable action board that appears when you start dragging a real file or folder, with up to 8 live drop zones.")
'''
          if old not in w:
              raise SystemExit("Drop card description marker missing")
          w = w.replace(old, new, 1)
          old = '''                    Label("File Shelf", systemImage: "tray")
'''
          new = '''                    Label("\(zones.configuration.zones.count) zone\(zones.configuration.zones.count == 1 ? "" : "s")", systemImage: "square.grid.2x2")
'''
          if old not in w:
              raise SystemExit("Drop card label marker missing")
          w = w.replace(old, new, 1)
          ws.write_text(w)

          # Remove EI pet/mascot from Retro Game CI and its setting, without touching EI elsewhere.
          pm = Path("Halo/Core/PersonalizationModels.swift")
          p = pm.read_text()
          mascot_storage = '    @AppStorage("HaloContextRetroCompanionMascot") private var showCompanionMascot = true\n'
          if p.count(mascot_storage) != 2:
              raise SystemExit(f"Expected 2 Retro mascot storage properties, found {p.count(mascot_storage)}")
          p = p.replace(mascot_storage, '')
          mascot_toggle = '            Toggle("Show EI companion mascot", isOn: $showCompanionMascot)\n'
          if p.count(mascot_toggle) != 1:
              raise SystemExit("Retro mascot toggle marker missing")
          p = p.replace(mascot_toggle, '')
          ei_observer = '    @ObservedObject private var eiSettings = EISettingsStore.shared\n'
          if p.count(ei_observer) != 1:
              raise SystemExit(f"Expected 1 Retro EI observer, found {p.count(ei_observer)}")
          p = p.replace(ei_observer, '')
          mascot_block = '''                if showCompanionMascot {
                      HaloCompanionSprite(
                          kind: eiSettings.settings.petKind,
                          style: .pixel,
                          size: 48,
                          primary: palette.foreground,
                          accent: eiSettings.settings.petAccentColor.color,
                          motion: game == .pong ? .look : .walk,
                          facingRight: false,
                          displayPreset: .clean,
                          pixelGrid: false,
                          pixelGlow: false,
                          scanlines: false,
                          ghosting: false,
                          brightnessVariation: false
                      )
                      .frame(width: 50, height: 38)
                      .opacity(0.70)
                      .padding(8)
                      .allowsHitTesting(false)
                  }
'''
          if p.count(mascot_block) != 1:
              raise SystemExit("Retro mascot render block missing")
          p = p.replace(mascot_block, '')
          pm.write_text(p)

          # One-shot workflow: remove itself from the resulting source commit.
          Path(".github/workflows/one-time-drop-ci-polish.yml").unlink()
          PY

      - name: Sanity check source changes
        shell: bash
        run: |
          set -euo pipefail
          ! grep -q 'summonFinderFallbackIfNeeded' Halo/NotchEngine/DisplayClock.swift
          ! grep -q 'HaloContextRetroCompanionMascot' Halo/Core/PersonalizationModels.swift
          ! grep -q 'Show EI companion mascot' Halo/Core/PersonalizationModels.swift
          grep -q 'struct HaloDropZoneSettingsEditor' Halo/NotchEngine/DisplayClock.swift
          grep -q 'HaloDropZoneSettingsEditor()' Halo/Views/WorkspaceSettingsView.swift
          git diff --check

      - name: Commit source changes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'Redstoneinvente'
          git config user.email '45894815+Redstoneinvente@users.noreply.github.com'
          git add Halo/NotchEngine/DisplayClock.swift Halo/Views/SurfaceView.swift Halo/Views/WorkspaceSettingsView.swift Halo/Core/PersonalizationModels.swift .github/workflows/one-time-drop-ci-polish.yml
          git commit -m 'Polish Drop CI and fix drag detection'
          git push origin HEAD:main
